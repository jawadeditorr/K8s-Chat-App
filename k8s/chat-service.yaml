apiVersion: v1
kind: Service
metadata:
  name: k8s-chat-app
  labels:
    app: k8s-chat-app
spec:
  type: NodePort
  selector:
    app: k8s-chat-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
      protocol: TCP
